version: "3"

tasks:
  test:
    desc: Run the full Go test suite
    cmds:
      - go test ./...

  test:coverage:
    desc: Run tests with coverage and print a per-package summary
    cmds:
      - go test -coverprofile=coverage.out ./...
      - go tool cover -func=coverage.out
